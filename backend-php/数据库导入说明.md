# 数据库导入说明

## 📊 SQL文件分析结果

### SQL文件信息

| 文件 | 大小 | 表数量 | 内容 |
|------|------|--------|------|
| `hkex_sm_com_20240321_230819.sql` | 约1594行 | 48个表 | FastAdmin框架表 + 业务表 |
| `hkex_sm_com_20240321_231454.sql` | 约1594行 | 48个表 | FastAdmin框架表 + 业务表 |

### 分析结论

**两个SQL文件都包含相同的FastAdmin框架表和业务表**，包括：
- `fa_admin` - 管理员表
- `fa_config` - 配置表
- `fa_user` - 用户表
- `fa_order` - 订单表
- 等等...

## 🗄️ 数据库分配建议

### 方案一：标准配置（推荐）

**后端数据库（hkex02）**：
- 导入两个SQL文件（完整数据）
- 这是FastAdmin后端使用的数据库
- 配置文件：`application/database.php`

**前端数据库（hkex01）**：
- **不需要导入SQL文件**
- uni-app前端通过API调用后端接口，不需要独立数据库
- 如果确实需要，可以导入其中一个SQL文件作为备份

### 方案二：如果前端需要独立数据库

如果您的项目架构中前端确实需要独立数据库，可以：

**前端数据库（hkex01）**：
- 导入：`hkex_sm_com_20240321_230819.sql`（第一个文件）
- 或导入：`hkex_sm_com_20240321_231454.sql`（第二个文件）
- 两个文件内容相同，任选一个即可

**后端数据库（hkex02）**：
- 导入两个SQL文件（完整数据）

## 📋 推荐导入方案

### 后端数据库（hkex02）- 必须导入

```bash
# 在宝塔面板中：
1. 找到数据库 hkex02
2. 导入第一个文件：hkex_sm_com_20240321_230819.sql
3. 导入第二个文件：hkex_sm_com_20240321_231454.sql
```

### 前端数据库（hkex01）- 可选

**情况1：前端不需要数据库（推荐）**
- 不需要导入任何SQL文件
- uni-app前端通过API调用后端

**情况2：前端需要数据库（如需要）**
- 导入其中一个SQL文件即可（两个文件内容相同）
- 建议导入：`hkex_sm_com_20240321_230819.sql`

## ⚠️ 重要说明

1. **uni-app前端通常不需要独立数据库**
   - 前端通过HTTP API调用后端接口
   - 后端FastAdmin处理所有数据库操作
   - 前端只负责展示和用户交互

2. **两个SQL文件内容相同**
   - 都是完整的数据库备份
   - 包含FastAdmin框架表和业务数据
   - 任选一个导入即可

3. **如果前端确实需要数据库**
   - 可能是项目特殊架构要求
   - 建议先导入一个SQL文件测试
   - 如果不需要，可以删除前端数据库

## 🎯 实际操作建议

### 第一步：导入后端数据库（hkex02）

**必须操作**：
1. 登录宝塔面板
2. 进入"数据库"管理
3. 找到数据库 `hkex02`
4. 点击"导入"
5. 导入两个SQL文件（按顺序）

### 第二步：前端数据库（hkex01）

**可选操作**：
- 如果前端不需要数据库：**不导入任何文件**
- 如果前端需要数据库：导入 `hkex_sm_com_20240321_230819.sql`（第一个文件）

## 📝 验证导入

### 后端数据库（hkex02）
```sql
-- 检查表数量
SHOW TABLES LIKE 'fa_%';

-- 检查管理员账号
SELECT * FROM fa_admin WHERE username='admin';
```

### 前端数据库（hkex01）
如果导入了SQL文件，验证方式同上。

---

**总结**：
- **hkex02（后端）**：导入两个SQL文件 ✅ 必须
- **hkex01（前端）**：通常不需要导入，如需导入则选择第一个SQL文件 ⚠️ 可选
