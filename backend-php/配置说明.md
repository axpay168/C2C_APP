# 项目配置说明

## 项目概述
- **后端框架**: FastAdmin (基于ThinkPHP)
- **前端框架**: uni-app (多语言支持)
- **域名**: MXTRX.TOP (HTTP，SSL证书尚未验证)

## 已完成的配置

### 1. 前端API地址配置
已更新以下文件中的API地址为 `http://MXTRX.TOP/api`:
- `/数据库前端/ouyuan 2/common/http.interceptor.js`
- `/数据库前端/ouyuan 2/store/index.js`

### 2. 后端跨域配置
已更新 `/application/config.php` 中的跨域域名配置:
```php
'cors_request_domain' => 'localhost,127.0.0.1,MXTRX.TOP,*.MXTRX.TOP',
```

### 3. 数据库配置
数据库配置文件位于 `/application/database.php`:
- 数据库名: `hkex_sm_com`
- 用户名: `hkex_sm_com`
- 密码: `6cGHKTh8wsA3beze`
- 表前缀: `fa_`

### 4. 后台入口
后台管理入口文件: `/public/buSRxMqJOo.php`
- 访问地址: `http://MXTRX.TOP/buSRxMqJOo.php`
- 默认账号: `admin`
- 默认密码: `123456`

## 待完成事项

### 1. Web服务器配置 (Nginx/Apache)
需要配置Web服务器，确保:
- 网站根目录指向 `/www/wwwroot/hkex-sm.com/public`
- 支持PHP (建议PHP 7.2+)
- 配置URL重写规则

#### Nginx配置示例:
```nginx
server {
    listen 80;
    server_name MXTRX.TOP;
    root /www/wwwroot/hkex-sm.com/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php-fpm/php-fpm.sock;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }
}
```

#### Apache配置:
已创建 `/public/.htaccess` 文件，确保Apache已启用mod_rewrite模块。

### 2. 数据库导入
需要导入数据库文件:
- `/数据库前端/hkex_sm_com_20240321_230819.sql`
- `/数据库前端/hkex_sm_com_20240321_231454.sql`

### 3. 文件权限设置
确保以下目录有写入权限:
- `/runtime/` - 运行时文件
- `/public/uploads/` - 上传文件目录

### 4. 前端构建部署
前端uni-app项目位于 `/数据库前端/ouyuan 2/`，需要:
- 安装依赖: `npm install` 或 `yarn install`
- H5构建: 使用HBuilderX或命令行构建
- 部署: 将构建后的文件部署到 `/public/h5/` 目录

### 5. SSL证书配置 (后续)
当SSL证书验证完成后，需要:
- 更新前端API地址为 `https://MXTRX.TOP/api`
- 更新后端跨域配置
- 配置HTTPS重定向

## 多语言支持
项目支持以下语言:
- `chn` - 简体中文
- `eng` - 英文
- `ko-kr` - 韩文
- `zh-hk` - 繁体中文

语言配置文件位于 `/数据库前端/ouyuan 2/common/locales/`

## 注意事项
1. 当前使用HTTP协议，SSL证书尚未验证
2. 后台入口文件名 `buSRxMqJOo.php` 为安全考虑，不建议修改
3. 确保PHP版本 >= 7.2.0
4. 需要安装Composer依赖: `composer install`
5. 前端项目需要Node.js环境进行构建

## 测试步骤
1. 访问后台: `http://MXTRX.TOP/buSRxMqJOo.php/index/login`
2. 使用默认账号登录: admin / 123456
3. 检查API接口: `http://MXTRX.TOP/api/index/index`
4. 访问前端H5: `http://MXTRX.TOP/h5/`
